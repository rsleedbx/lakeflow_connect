# Databricks provider - use default (host/token from env or ~/.databrickscfg)

provider "databricks" {
  # host  = ...  # from env DATABRICKS_HOST or config
  # token = ...  # from env DATABRICKS_TOKEN or config
}
